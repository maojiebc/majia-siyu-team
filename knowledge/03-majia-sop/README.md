# 第三层 · 真实 SOP（护城河 · 私有）

> ⚠️ 本目录放 14 年操盘攒下的真实 SOP，是整个专家团的护城河。
> **除本 README 外的所有内容 git-ignore，绝不进公开库**（见根 `.gitignore`）。
> 语义检索接口已预留（`src/siyu_team/connectors/nowledge_mem.py`，未接入）；
> 当前四个专家 skill 通过 `tools/atoms_query.py` 直查本层 `atoms.jsonl` 取数。

放什么：
- 真实加微话术 / 分层阈值 / 活动节奏配比 / 复购召回剧本
- 真实操盘案例数据（脱敏后才可对外引用）
- 各行业（餐饮先行，再零售/教培）的实操做法清单（playbook）
