# 飞书多维表格字段说明（v1.5.1 共创管线）

目标：一张表收投稿，一张表收印证。每日 GitHub Action 拉取后写入 `knowledge/05-community/`，下次构建自动带上。

维护者只需在飞书里建好表和表单；字段名必须与下表一致，否则拉取对不上。公开表单 15 个字段 + 系统回写列。`我也遇到过` 是回链，不进表单。

## 表一：`案例提交`（表单写入）

公开表单只绑定这一张表。

| 序号 | 字段名 | 飞书类型 | 必填 | 选项 / 说明 |
|---:|---|---|---|---|
| 1 | 业态 | 单选 | 是 | 餐饮·正餐 / 餐饮·快餐小吃 / 餐饮·火锅烧烤 / 咖啡茶饮 / 烘焙甜品 / 零售·便利店 / 零售·商超生鲜 / 零售·服饰美妆 / 零售·母婴宠物 / 其他。旧值「餐饮」「零售」仍认 |
| 2 | 细分 | 文本 | 否 | 有则接到业态标签后面，如「餐饮·正餐 · 川菜」 |
| 3 | 经营模式 | 单选 | 是 | 直营 / 加盟 / 混合 |
| 4 | 门店数 | 单选 | 是 | 1 / 2-10 / 11-50 / 51-300 / 301-1000 / 1001-5000 / 5000+。不要再用 300+ |
| 5 | 分公司层 | 单选 | 否 | 有 / 没有 / 不清楚 |
| 6 | 你的位置 | 单选 | 否 | 总部 / 分公司或区域 / 加盟商 / 门店 / 服务商或顾问 |
| 7 | 主要私域载体 | 多选 | 否 | 企业微信 / 个人微信 / 微信社群 / 小程序会员 / 公众号 / 抖音快手私信 / 小红书 / 其他 |
| 8 | 这条属于 | 单选 | 是 | 平台技巧 / 文案钩子 / 排版 / 发送时机 / 数字基线 / 反例翻车 / 厂商体验 / 规则变化 / 方法 |
| 9 | 一件有效的事（或没用的事） | 多行文本 | 是 | 判断句正文 |
| 10 | 数字/证据 | 文本 | 否 | 如：加微率 12%→19%，n=37 店 |
| 11 | 发现时间 | 日期 | 是 | 你观察到这件事的日期 |
| 12 | 现在还有效吗 | 单选 | 是 | 是 / 不确定 / 已失效 |
| 13 | 公司/品牌 | 文本 | 是 | 入库前哈希，仓库不存原文 |
| 14 | 联系方式 | 文本 | 否 | 只留在飞书，永不进仓库 |
| 15 | 对外显示名 | 文本 | 否 | 公开署名，按设计镜像到公开 Base，不做哈希。空则按「匿名同行」。手机号/邮箱等过不了 PII 扫描 |

回链列（表单不要展示）：`我也遇到过` 关联到 `印证`（`fldeUzJgnq`）。投稿人走独立「印证」表。

评审列（表单不要展示；Base 协作者在飞书里填，管线只读）：

| 字段名 | 飞书类型 | 取值 |
|---|---|---|
| 评审结论 | 单选 | 待审 / 通过 / 驳回。空视为待审 |
| 评审等级 | 单选 | A / B / C / D。通过时必填；机器不改这一列 |
| 评审批注 | 多行文本 | 驳回时写一句原因；通过也可留备注 |
| 评审人 | 文本 | 也可是人员字段（认 `name`）。空则记为 `maintainer` |

系统列（表单不要展示，Action 回写）：

| 字段名 | 飞书类型 | 取值 |
|---|---|---|
| 状态 | 单选 | 待审 / 已通过 / 已驳回 / 需人工 / 撤销。旧值「已收录D / 已晋级C / A级」只在「评审结论=通过」时按已通过理解，否则按待审；下次回写改成新选项 |
| 原子ID | 文本 | `ka_` + 16 位十六进制 |
| 回礼 | 多行文本 | 结构化案例卡 + 评审行 + 同规模带基线摘要 |
| 建议等级 | 文本 | 机器建议：维护者 ID→A；≥2 家独立公司印证→C；否则 D。不是发布等级 |
| 印证数 | 数字 | 独立印证条数（不含原投稿） |
| 创建时间 | 系统时间 | 用于 H3：提交到评审通过的小时数 |

环境变量里的表 ID：`LARK_TABLE_SUBMISSIONS`。

`SIYU_INTAKE_MODE`（仓库变量或 Secret）：未设或 `lenient` 时，未知类别/坏日期/PII 脱敏后仍收录（原文 PII 不入库）；`strict` 才把这三类打成「需人工」。把「状态」改成 `撤销`，下次跑会删仓库里对应原子、回礼写成「已撤销」，并记入 `revoked.jsonl`，不再重收。评审结论改成「通过 / 驳回」后再跑一次，原子会从 `pending.jsonl` 挪到 `approved.jsonl` 或 `rejected.jsonl`，不新建第二条。

## 表二：`印证`

只收集证据。≥2 家不同公司独立确认会让机器把「建议等级」写成 C，**不会**自动发布。发布看表一的「评审结论」。

| 字段名 | 飞书类型 | 必填 | 说明 |
|---|---|---|---|
| 关联提交 | 关联「案例提交」（`fldPzRUryg`） | 否 | 维护者事后手链。公开表单不能暴露关联字段 |
| 印证的原子ID | 文本 | 表单侧必填 | 印证人从案例卡/回礼里复制 `ka_…` 贴上 |
| 印证人公司 | 文本 | 是 | 入库前哈希，必须和原投稿不是同一家 |
| 印证时间 | 日期 | 是 | 印证人确认的日期 |
| 对外显示名 | 文本 | 否 | 公开署名，按设计镜像到公开 Base，不做哈希。空则按「匿名同行」 |

环境变量：`LARK_TABLE_CONFIRMATIONS`（已是 `tblclv6LhLPIKQP4`）。解析顺序：关联 `record_id` → `印证的原子ID`（去空白、小写，有无 `ka_` 都行）→ 关联展示文本。对不上的记入 `confirmations_unresolved`，不中断。

同一判断句被另一家公司再交一版（去重 Jaccard ≥ 0.8）也算印证，不要求只走这张表。

## 表三：`公开判断`（派生，勿手改）

独立公开 Base，不和投稿表做跨 Base 关联。每日 Action 按 `来源记录` = 私有 `record_id` 整表检索一次后批量创建/更新；没变的行跳过。这张表由管线派生，不要手改。

环境变量：`LARK_PUBLIC_BASE_TOKEN`（`JH2HbehyqaYoRtspxE4c5qVInmd`）、`LARK_PUBLIC_TABLE`（`tblakszImmH7GaTg`）。

| 字段名 | 飞书类型 | 说明 |
|---|---|---|
| 判断 | 文本（主键） | 脱敏后的判断句，永不写原文 PII |
| 原子ID | 文本 | `ka_…` |
| 状态 | 单选 | 待审 / 已通过。旧值可能仍在；管线按私有「评审结论」重写 |
| 这条属于 | 文本 | 表单原值 |
| 业态 | 文本 | 表单原值 |
| 细分 | 文本 | 表单原值；含 PII 则留空 |
| 经营模式 | 文本 | 表单原值 |
| 门店数 | 文本 | 表单原值 |
| 分公司层 | 文本 | 表单原值 |
| 你的位置 | 文本 | 表单原值 |
| 主要私域载体 | 多选 | 表单原值 |
| 数字/证据 | 文本 | 脱敏后的证据，空则空 |
| 发现时间 | 日期 | 表单发现时间 |
| 现在还有效吗 | 文本 | 表单原值 |
| 提交时间 | 日期 | 私有「创建时间」 |
| 来源记录 | 文本 | 私有 `record_id`，镜像主键 |
| 对外显示名 | 文本 | 公开署名；空则「匿名同行」 |
| 等级 | 文本 | 待审行：`待审（建议X）`；已通过行：`评审通过·X级` |
| 印证数 | 数字 | 独立印证条数（不含原投稿） |

没有 `公司/品牌`、`联系方式`、`回礼`。`撤销` / `已驳回` / `已拒` / `需人工` → 删除该公开行。待审行会公开（等级写成建议，不进 skill）。

## 飞书 CellValue 形态

拉记录时按实际 API 形态解析，不要假设只有一种：

| 字段类型 | 常见返回 | 也要认 |
|---|---|---|
| 单选 | `["餐饮·正餐"]` | 纯字符串；旧值 `["餐饮"]` |
| 多选 | `["企业微信","小程序会员"]` | 纯字符串或带 `text` 的片段 |
| 日期 | `"2026-09-03T00:00:00.000+08:00"` | 毫秒时间戳（按 `Asia/Shanghai` 取日历日） |
| 关联 | 空=`null`；有值=`[{"id":"rec…"}]` | `record_id` / `record_ids` / `text` |
| 文本 / 多行文本 | 纯字符串 | `[{"text":"…","type":"text"}]` 片段 |

## 表单字段 → 原子字段

| 表单 | KnowledgeAtomV2 |
|---|---|
| 一件有效的事（或没用的事） | `statement` |
| 这条属于 | `type`（平台技巧→`platform_workaround`，文案钩子→`hook_pattern`，排版→`layout_pattern`，发送时机→`timing`，数字基线→`benchmark`，反例翻车→`anti-pattern`，厂商体验→`vendor_experience`，规则变化→`rule_change`，方法→`method`） |
| 业态 / 细分 | `scope.industry` / `scope.subindustry`。前缀 `餐饮·`、`咖啡茶饮`、`烘焙甜品`→`catering`；前缀 `零售·`→`retail`；`其他`→`other`。旧值「餐饮」「零售」仍认。细分有则接到标签后 |
| 经营模式 | `scope.business_model`（直营→direct，加盟→franchise，混合→mixed） |
| 门店数 | `scope.scale_band`（旧值 `300+` 记为 `any`） |
| 分公司层 | `scope.org_layers`（有→regional，没有→none，不清楚→any） |
| 你的位置 | `source.contributor_role`（总部→hq，分公司或区域→regional，加盟商→franchisee，门店→store，服务商或顾问→vendor_or_consultant；缺省 unknown） |
| 主要私域载体 | `scope.channels`（企业微信→wecom，个人微信→personal_wechat，微信社群→wechat_group，小程序会员→miniprogram_member，公众号→official_account，抖音快手私信→douyin_kuaishou_dm，小红书→xiaohongshu，其他→other） |
| 数字/证据 | `applicability.metrics[]`；空则写「指标不适用」 |
| 发现时间 | `source.observed_at`、`lifecycle.valid_from` |
| 现在还有效吗=已失效 | `valid_until`=发现当日（立即过期，仍留档） |
| 平台技巧 / 规则变化 | 180 天窗：`是`→创建时间（缺则今天）+180 天；`不确定`→发现时间+180 天，且不早于今天+30 天；`已失效`→发现当日。印证到达则 `valid_until` 延到印证日+180 天。`quality.platform_rule_risk` 由关键词扫描标注，不拒收 |
| 公司/品牌 | `quality.confirmations[].company_hash` |
| 联系方式 | 不入库；仅用来匹配 `SIYU_MAINTAINER_IDS` |
| 对外显示名 | `source.contributor_display_name`；印证写入 `quality.confirmations[].display_name`。空=匿名同行。公开字段，不哈希；含手机号/邮箱时宽松改匿名并备注，严格需人工 |

原子 `id` = `sha256(归一化判断句 + company_hash)` 的前 16 位，前缀 `ka_`。

## 哈希规则

- 环境变量 `SIYU_HASH_SALT`：仓库级盐，至少 16 字符，不入库。
- 公司名先去括号地区、法律/业态后缀（如有限公司、集团、（上海）、上海分公司），再去空白标点并转小写，再算 `sha256(盐 + 换行 + 归一化名称)`。
- 贡献者哈希用「联系方式」；没有联系方式时用记录 ID。原文都不进 JSONL。
- `印证.关联提交` 先绑 `record_id`/`id`（`rec_…`）；表单侧请填 `印证的原子ID`；都没有才用关联展示文本。

## 收录与评审

机器只收证据、给建议，不决定发布。

- 能过校验的投稿先写入 `pending.jsonl`，`quality.review_status=pending`，`evidence_grade` / `suggested_grade` 都是机器建议，状态回写 `待审`。回礼多一行「评审：待审，通过后进入下一版 skill」。
- 建议等级：联系方式命中 `SIYU_MAINTAINER_IDS` → A；≥2 个不同公司（原投稿 + 独立印证）→ C；否则 D。自评、同公司第二条不计。建议写回飞书「建议等级」「印证数」。
- 「评审结论=通过」→ `approved.jsonl`，发布等级 =「评审等级」，`reviewer` =「评审人」或 `maintainer`，状态 `已通过`，回礼「评审：已通过（X级）」。只有这一档会进 skill / 公开装配。
- 「评审结论=驳回」→ `rejected.jsonl`（id、record_id、时间、评审人、批注），状态 `已驳回`，之后不再重收，除非改回「通过」。
- 「状态=撤销」仍走 `revoked.jsonl`，与驳回分开。
- 判断句、证据或对外显示名里扫到手机号、身份证、邮箱、密钥、门店编号：严格 → 状态 `需人工`；宽松 → 判断句/证据就地脱敏、显示名改「匿名同行」并备注。原文 PII 不入库。
- 平台技巧只打风险标签，宽松照收，仍待评审。

## 旧版（v1.2.x，已弃用）

v1.2.1 公开表曾是 13 题（7 个核心问题 + 5 项元数据 + 1 个附件），同步到「案例提交与审核（v1.2.1）」，不接 API、不自动入库。那套表和审核分钟统计只作历史记录；新投稿走本文件的两张表。
